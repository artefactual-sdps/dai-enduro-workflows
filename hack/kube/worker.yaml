apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: dai-enduro-worker
  labels:
    app: dai-enduro-worker
spec:
  serviceName: dai-enduro-worker
  selector:
    matchLabels:
      app: dai-enduro-worker
  template:
    metadata:
      labels:
        app: dai-enduro-worker
    spec:
      serviceAccountName: sdps
      securityContext:
        fsGroup: 1000
      containers:
        - name: dai-enduro-worker
          image: dai-enduro-worker:dev
          volumeMounts:
            - name: config
              mountPath: /home/enduro/.config
              readOnly: true
            - name: shared-dir
              mountPath: /home/enduro/shared
          resources: {}
      volumes:
        - name: config
          secret:
            secretName: dai-enduro-secret
        - name: shared-dir
          persistentVolumeClaim:
            claimName: preprocessing-pvc
