apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: custom-enduro-worker
  labels:
    app: custom-enduro-worker
spec:
  serviceName: custom-enduro-worker
  selector:
    matchLabels:
      app: custom-enduro-worker
  template:
    metadata:
      labels:
        app: custom-enduro-worker
    spec:
      serviceAccountName: sdps
      securityContext:
        fsGroup: 1000
      containers:
        - name: custom-enduro-worker
          image: custom-enduro-worker:dev
          volumeMounts:
            - name: config
              mountPath: /home/enduro/.config
              readOnly: true
            - name: shared-dir
              mountPath: /home/enduro/shared
          resources: {}
      volumes:
        - name: config
          secret:
            secretName: custom-enduro-secret
        - name: shared-dir
          persistentVolumeClaim:
            claimName: preprocessing-pvc
